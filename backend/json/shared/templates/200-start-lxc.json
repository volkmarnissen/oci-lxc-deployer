{
  "execute_on": "proxmox",
  "name": "Start LXC Container",
  "description": "Start existing LXC container on Proxmox host",
  "parameters": [
    {
      "id": "vm_id",
      "name": "VM ID",
      "type": "number",
      "description": "ID of the virtual machine"
    }
  ],
  "commands": [
    {
      "command": "pct start {{ vm_id }} 1>&2"
    }
  ]
}
